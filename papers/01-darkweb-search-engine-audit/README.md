# Paper 01 — A Single-Snapshot Audit of 18 Tor Search Engines

> **ResearchGate publication:** _(link pending upload)_

A single-snapshot, pre-registered audit of eighteen Tor-network search
engines reached through a freshly bootstrapped Tor Expert Bundle on
2026-05-28. The paper reports per-engine liveness, latency, and a silent
parser-drift failure mode in the dispatcher's Ahmia handler. It is
deliberately not a benchmark in any IR sense.

## What is in this directory

| Path | Content |
|---|---|
| `figures/fig1_pipeline.mmd` / `.png` | Mermaid + rendered PNG of the measurement pipeline |
| `figures/fig2_latency_distribution.png` | Bar chart of Q0 engine latencies (generated by `scripts/`) |
| `figures/fig3_silent_failure_taxonomy.mmd` / `.png` | Silent-vs-loud failure decision tree |
| `figures/fig4_circuit_isolation.mmd` / `.png` | Single-circuit vs rotated-circuit hygiene |
| `scripts/make_fig2_latency_distribution.py` | matplotlib script that generates Figure 2 from `supplementary/q00_precheck.json` |
| `supplementary/` | The artefact bundle attached to the paper on ResearchGate as Supplementary S1 |
| `references/_style_notes.md` | Field-specific writing conventions used for the paper |
| `CONTEXT.md` | Raw findings and measurement methodology notes |

## Supplementary bundle (`supplementary/`)

These files are also distributed as `supplementary_S1.zip` with the
ResearchGate publication.

- `T2_tor_setup.md` — isolated Tor instance setup, bootstrap log,
  `IsTor: true` verification
- `T3_install_report.md` — Python venv install transcript, smoke tests
- `T3_pip_freeze.txt` — exact pinned dependency manifest
- `torrc` — the configuration file actually used
- `onion_t4b.py` — the five-query catalogue script
- `q00_precheck.json` through `q05_multi_engine_osint.json` — raw query outputs
- `_run.log` — combined run log
- `audit_summary.md` — pre-install source audit of the OnionClaw dispatcher
- `legacy_check.py` / `legacy_check.txt` / `legacy_check.json` — replication
  probe verifying that two of Faizan & Khan (2019)'s six engines still resolve

## Reproducibility

Independent re-execution against a fresh Tor circuit set takes approximately
three minutes. See §5.4 of the paper for the full reproducibility checklist
and `audit_summary.md` for the dispatcher provenance.
